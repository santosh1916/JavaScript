let a = document.getElementsByTagName('div')[0]
// a.innerHTML= a.innerHTML+'<h1>Hello dear!</h1>';

let div = document.createElement('div');
div.innerHTML = '<h1>Hello this is new div</h1>';
// document.body.append(div) // append in body
// a.appendChild(div)// append in child of a
// a.append(div); // display after div 
// a.prepend(div) // display before div
// a.replaceWith(div) // repalce any tag
